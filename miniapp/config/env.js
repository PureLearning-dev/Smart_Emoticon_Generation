/**
 * 环境配置
 * 职责：统一管理后端服务地址，便于不同环境切换
 *
 * 开发说明：
 * - 开发者工具调试：使用 127.0.0.1:8080 即可，需在「详情」→「本地设置」中勾选「不校验合法域名」。
 * - 真机预览/真机调试：127.0.0.1 指向手机本机，需改为本机内网 IP，如 http://192.168.x.x:8080。
 * - 正式环境：改为已备案的 https 域名，并在微信公众平台配置 request 合法域名。
 */
const ENV = {
  // 微信开发者工具调试时，可替换为你的内网穿透域名或可访问地址
  baseUrl: "http://127.0.0.1:8080"
};

module.exports = ENV;
