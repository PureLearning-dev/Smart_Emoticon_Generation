# 复制为 .env 后修改；不要提交 .env
# 留空则开发时走 Vite proxy：请求 /api 转发到 http://127.0.0.1:8080
VITE_API_BASE_URL=

# 演示登录（仅前端校验，非后端真实账号体系）
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=admin123

# 当前后端开发环境通常 permitAll，默认不要带 mock Authorization，避免跨域预检或无效 token 干扰。
VITE_SEND_AUTH_HEADER=false
